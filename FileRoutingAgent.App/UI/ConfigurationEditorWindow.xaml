<Window x:Class="FileRoutingAgent.App.UI.ConfigurationEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration Editor"
        Height="780"
        Width="1300"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="8" Margin="0,0,0,8">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PolicyPathText" FontWeight="SemiBold" />
        <TextBlock x:Name="SignaturePathText" Grid.Row="1" Margin="0,4,0,0" />
        <TextBlock x:Name="SignatureStatusText" Grid.Row="2" Margin="0,4,0,0" />
      </Grid>
    </Border>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*" />
        <ColumnDefinition Width="2*" />
      </Grid.ColumnDefinitions>

      <GroupBox Header="Policy JSON" Grid.Column="0" Margin="0,0,8,0">
        <TextBox x:Name="PolicyJsonTextBox"
                 FontFamily="Consolas"
                 FontSize="13"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap" />
      </GroupBox>

      <Grid Grid.Column="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Header="Validation Results" Margin="0,0,0,8">
          <ListBox x:Name="ValidationListBox" />
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Status">
          <TextBox x:Name="StatusTextBox"
                   IsReadOnly="True"
                   AcceptsReturn="True"
                   VerticalScrollBarVisibility="Auto"
                   TextWrapping="Wrap" />
        </GroupBox>
      </Grid>
    </Grid>

    <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button Width="140" Margin="4" Content="Reload From Disk" Click="ReloadButton_OnClick" />
      <Button Width="120" Margin="4" Content="Validate" Click="ValidateButton_OnClick" />
      <Button Width="130" Margin="4" Content="Guided Setup..." Click="GuidedSetupButton_OnClick" />
      <Button Width="170" Margin="4" Content="Add Project Template" Click="AddProjectButton_OnClick" />
      <Button Width="220" Margin="4" Content="Apply PW Cmd Profile (Unconfigured)" Click="ApplyProjectWiseProfileButton_OnClick" />
      <Button Width="90" Margin="4" Content="Save" Click="SaveButton_OnClick" />
      <Button Width="170" Margin="4" Content="Save + Sign + Reload" Click="SaveSignReloadButton_OnClick" />
      <Button Width="90" Margin="4" Content="Close" Click="CloseButton_OnClick" />
    </WrapPanel>
  </Grid>
</Window>
